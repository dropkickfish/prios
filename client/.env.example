# No client-side environment variables are required for normal operation.
# Auth is handled server-side via session cookies — no tokens needed here.

# Legacy: if you set API_KEY in server/.env and are NOT using AUTH_ENABLED,
# set the same value here so the browser UI can authenticate.
# Prefer creating a named Personal Access Token in Settings > API Keys instead.
# VITE_API_KEY=
